# Release notes proposal

Summarize user-visible changes from the supplied commit range. Cite the changed
files, separate verified behavior from inference, and write only to the proposal
branch supplied by C6. Never print environment variables or credential values.
